version: '3.8'

services:
  aniseerr:
    image: ghcr.io/yezun-hikari/aniseerr:latest
    container_name: aniseerr
    ports:
      - "5010:5010"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
